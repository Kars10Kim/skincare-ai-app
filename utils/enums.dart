/// Camera mode to determine the current scanning mode
enum CameraMode {
  /// Barcode scanning mode for product identification
  BARCODE,
  
  /// Text recognition mode for ingredient labels
  TEXT
}